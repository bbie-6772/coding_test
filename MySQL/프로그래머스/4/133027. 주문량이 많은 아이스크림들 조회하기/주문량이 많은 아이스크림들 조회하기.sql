-- 코드를 입력하세요
WITH TOTAL_SUM AS(
SELECT  FLAVOR
        ,SUM(TOTAL_ORDER) AS TOTAL
FROM    
(
SELECT  FLAVOR,
        TOTAL_ORDER
FROM    JULY
UNION
SELECT  FLAVOR,
        TOTAL_ORDER
FROM    FIRST_HALF
) COMBINED
GROUP BY 1
ORDER BY 2 DESC
)
SELECT FLAVOR
FROM TOTAL_SUM
LIMIT 3

